# Bundle-size report: `@stellar/stellar-sdk` 17.0.0 vs 17.0.0-rc.2

Generated by `npm run benchmark` on 2026-08-21 (Node v24.13.0, esbuild 0.25.12). baseline = `@stellar/stellar-sdk@17.0.0-rc.2`, candidate = `@stellar/stellar-sdk@17.0.0`. Sizes are of a production esbuild bundle (minified, ESM, `platform: browser`). gzip is the number users download; it is the one to quote.

## Size by scenario

| Scenario | 17.0.0-rc.2 gzip | 17.0.0 gzip | Δ gzip | candidate brotli | candidate raw |
| --- | --- | --- | --- | --- | --- |
| Full SDK (`import * as`) | 178.1 KB | 178.3 KB | +0.2% | 145.9 KB | 802.2 KB |
| RPC only (`/rpc`) | 134.2 KB | 134.4 KB | +0.1% | 110.2 KB | 627.2 KB |
| Classic Horizon app | 102.7 KB | 102.9 KB | +0.2% | 85.3 KB | 461.0 KB |
| Contract client (`/contract`) | 134.1 KB | 134.3 KB | +0.2% | 110.1 KB | 627.2 KB |

Negative Δ means the candidate is smaller. **These are default-resolution numbers:** each version is bundled the way a real bundler would resolve it. The baseline ships no prebuilt `browser` root, so both versions resolve ESM source in every scenario. These deltas are a like-for-like code-size comparison; the control below is a secondary check under `platform: node`.

## Build-input fairness (control: bundle from source)

Both versions already resolve source by default, so this control mainly re-checks the headline numbers under different build settings rather than correcting for a UMD baseline. These control builds use `platform: node` (node built-ins external), so they are NOT directly comparable to the `platform: browser` rows above — compare baseline-source to candidate-source *within* this table.

| Scenario | baseline default | baseline source (ESM) | candidate source (ESM) | Δ gzip (candidate vs baseline source) |
| --- | --- | --- | --- | --- |
| full | 178.1 KB | 178.2 KB | 178.5 KB | +0% |
| horizon-classic | 102.7 KB | 102.9 KB | 103.0 KB | +0% |

**Summary:** bundled from source (baseline ESM vs candidate ESM), the candidate is **0% larger** for the full SDK and **0% larger** for a classic Horizon import. Subpath imports (`/rpc`, `/contract`) show 0–0% larger. Both versions resolve source in the default numbers too, so the headline table and this control point the same direction.

## Tree-shaking watch

Does a narrow import still drag in heavy or Horizon-only deps? A ✓ in a non-full scenario means tree-shaking left that module in.

| Scenario | version | eventsource | smol-toml | axios | feaxios | buffer |
| --- | --- | --- | --- | --- | --- | --- |
| Full SDK (`import * as`) | 17.0.0-rc.2 | ✓ | ✓ | · | ✓ | · |
| Full SDK (`import * as`) | 17.0.0 | ✓ | ✓ | · | ✓ | · |
| RPC only (`/rpc`) | 17.0.0-rc.2 | · | · | · | ✓ | · |
| RPC only (`/rpc`) | 17.0.0 | · | · | · | ✓ | · |
| Classic Horizon app | 17.0.0-rc.2 | ✓ | ✓ | · | ✓ | · |
| Classic Horizon app | 17.0.0 | ✓ | ✓ | · | ✓ | · |
| Contract client (`/contract`) | 17.0.0-rc.2 | · | · | · | ✓ | · |
| Contract client (`/contract`) | 17.0.0 | · | · | · | ✓ | · |

Takeaway: the candidate's tree-shaking is real (`eventsource`/`smol-toml` drop out of narrower imports), but not yet clean — `feaxios` still rides along in every candidate bundle.

## Caveats

- Each version is resolved the way a real bundler would resolve it (default export conditions). Neither version ships a prebuilt `browser` root, so both resolve source in every scenario and the watch column is meaningful for both.
- `platform: browser`. A scenario that fails to bundle shows a failure row in the size table above, and is itself a finding (it needs polyfills in the browser).

